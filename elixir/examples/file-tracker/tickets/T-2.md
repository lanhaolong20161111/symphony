---
id: T-2
title: Ship the release notes
state: ready
blocked_by: [T-1]
---

Held back until `T-1` is done: this ticket reports `dispatchable: false`, so the orchestrator
leaves it in the blocked list instead of handing it to an agent.

Delete the `blocked_by:` line (or set `state: done` on `T-1`) when you want it picked up.
