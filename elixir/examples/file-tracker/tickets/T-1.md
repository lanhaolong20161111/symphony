---
id: T-1
title: Cache the git roots lookup
state: ready
labels: [perf]
priority: 2
---

`git_roots/0` walks the filesystem on every call. Cache it per run and add a test that
proves the second call does not touch the disk.

This body is what the agent receives as the issue description, so write it like a task
brief: what to change, and how to know it worked.
